[package]
name = "fbsd-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe(r) Rust driver framework on top of fbsd-sys: module lifecycle, kernel_module! macro, printf(9)/panic(9) wrappers."

[lib]
name = "fbsd_kernel"

[dependencies]
fbsd-sys = { path = "../fbsd-sys" }
