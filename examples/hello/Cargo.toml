[package]
name = "hello"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
name = "hello"
crate-type = ["staticlib"]

[dependencies]
fbsd-kernel = { path = "../../crates/fbsd-kernel" }
